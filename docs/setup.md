# Installation - Setup

## Install Node.js

Install Node.js (LTS version).

Verify installation:

```bash
node -v
npm -v
```

Expected:
Commands should print the installed version numbers.
